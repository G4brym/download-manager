#!/usr/bin/with-contenv bash

if [ ! -f "/app/downloads/db.sqlite3" ]; then
	ln -s /app/downloads/db.sqlite3 /config/db.sqlite3
fi

mv /defaults/uwsgi.ini /app/downloads/uwsgi.ini > /dev/null 2>&1

# permissions
chown -R abc:abc \
	/app/downloads \
	/config \
	/downloads
